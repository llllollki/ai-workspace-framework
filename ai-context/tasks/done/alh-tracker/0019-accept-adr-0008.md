Status: done
Created: 2026-05-19
Owner role: Documentation maintainer
Reviewers: n/a

## Goal

Accept ADR 0008 (CRM-to-tracker provisioning API authentication) and clean up all documentation references that still describe it as proposed.

## Acceptance Criteria

- [x] ADR 0008 status is `accepted` in both mirrors (`ai-context` and `ai-workspace-framework`)
- [x] `decisions/README.md` lists ADR 0008 as `Accepted` in both mirrors
- [x] `ai_memory.md` ADR 0008 reference updated from `proposed` to `accepted` in both mirrors
- [x] `data_model.md` ADR 0008 reference updated from `proposed` to `accepted` in both mirrors
- [x] `execution_log.md` updated in both mirrors
- [x] Task doc created and moved to done in both mirrors
- [x] Changes committed to `ai-workspace-framework`
- [x] No application code changed

## Plan

- [x] Update ADR 0008 `**Status:**` from `proposed` to `accepted` in both mirrors
- [x] Update ADR 0008 row in `decisions/README.md` from `Proposed` to `Accepted` in both mirrors
- [x] Update `ai_memory.md` ADR 0008 reference from `proposed` to `accepted` in both mirrors
- [x] Update `data_model.md` ADR 0008 reference from `proposed` to `accepted` in both mirrors
- [x] Add one-line acceptance entry to `execution_log.md` in both mirrors
- [x] Create this task doc directly in done in both mirrors
- [x] Commit changes to `ai-workspace-framework`

## Outcome

ADR 0008 accepted 2026-05-19 following architecture review (task 0018, recommendation: Accept with minor edits). Status updated in both mirrors. `decisions/README.md`, `ai_memory.md`, and `data_model.md` updated in both mirrors. No application code changed. No Supabase schema changes. Changes committed to `ai-workspace-framework`.
