# Active Tasks — AssistedLivingHelp

This directory contains task documents currently in progress for AssistedLivingHelp.

Each task document should follow the structure defined in `ai-context\orchestration\planning_rules.md`.

When a task is complete, move it to `ai-context\tasks\done\AssistedLivingHelp\`.
