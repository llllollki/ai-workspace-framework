# Backlog Tasks — AssistedLivingHelp

This directory contains task documents queued for future work on AssistedLivingHelp.

Each task document should follow the structure defined in `ai-context\orchestration\planning_rules.md`.

When a task is started, move it to `ai-context\tasks\active\AssistedLivingHelp\`.
